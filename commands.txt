cd ~/Desktop
unzip "sandbox2023.zip"
cd sandbox
ls

git init -b master
git status
echo "*.RData" > .gitignore
echo ".DS_Store" >> .gitignore
cat .gitignore
git status
git add .
git status
git commit -m "1"
git remote add origin https://github.com/Chuning-S/sandbox
git push -u origin master

#I disclose the use of AI (ChatGPT version: GPT-5.6 sol)for helping me solve an authentication issue when adding to the remote repository)
git config --get credential.helper
git credential-osxkeychain erase
host=github.com
protocol=https
git remote add origin https://github.com/Chuning-S/sandbox.git
git push -u origin master

git branch playground
git checkout playground
git branch
git rm -r data/update2019
git status
echo "the one line can say whatever" > sandbox_todo.txt
git add sandbox_todo.txt
git status
git commit -m "commit message#"
git push -u origin playground

git switch master
git rm -r data/zohnerdata
git rm -r analyses/cleaning/zohner_cleaning
echo "one line note" > data/dailyclimatedata_WHERE.txt
git status
echo "another one line note" > analyses/cleaning/_cleaningREADME.txt
git status
git add .
git commit -m "task (3)"
git push

git switch playground
git rm -r analyses/cleaning/misccleaning_adventures
git commit -m "task (4)"
git push

git switch master
